# ==============================================================================
# Experience Hub 크롤러 필수 라이브러리 목록
# 설치 방법: pip install -r requirements.txt
# ==============================================================================

# Supabase 데이터베이스와 통신하기 위한 공식 파이썬 라이브러리
supabase>=2.0.0

# 웹 페이지 요청 및 API 통신을 위한 라이브러리
requests>=2.31.0

# HTML 문서를 파싱하고 원하는 태그/데이터를 추출하는 라이브러리
beautifulsoup4>=4.12.0

# .env 파일에서 환경변수(DB 접속 정보 등)를 안전하게 불러오는 라이브러리
python-dotenv>=1.0.0

# 최신 브라우저 환경의 동적 렌더링 사이트 수집용 (선택 사항)
playwright>=1.40.0
